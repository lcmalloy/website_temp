export type LinksDetails = {
    path: string;
    isDropdownMenu: boolean;
    addLinks?: string[];
  }[];

export type LinkDetails = {
    path: string;
    isDropdownMenu: boolean;
    addLinks?: string[];
  };

